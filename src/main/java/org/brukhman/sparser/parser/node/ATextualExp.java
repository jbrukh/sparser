/* This file was generated by SableCC (http://www.sablecc.org/). */

package org.brukhman.sparser.parser.node;

import java.util.*;
import org.brukhman.sparser.parser.analysis.*;

@SuppressWarnings("nls")
public final class ATextualExp extends PExp
{
    private final LinkedList<PTextual> _textual_ = new LinkedList<PTextual>();

    public ATextualExp()
    {
        // Constructor
    }

    public ATextualExp(
        @SuppressWarnings("hiding") List<PTextual> _textual_)
    {
        // Constructor
        setTextual(_textual_);

    }

    @Override
    public Object clone()
    {
        return new ATextualExp(
            cloneList(this._textual_));
    }

    public void apply(Switch sw)
    {
        ((Analysis) sw).caseATextualExp(this);
    }

    public LinkedList<PTextual> getTextual()
    {
        return this._textual_;
    }

    public void setTextual(List<PTextual> list)
    {
        this._textual_.clear();
        this._textual_.addAll(list);
        for(PTextual e : list)
        {
            if(e.parent() != null)
            {
                e.parent().removeChild(e);
            }

            e.parent(this);
        }
    }

    @Override
    public String toString()
    {
        return ""
            + toString(this._textual_);
    }

    @Override
    void removeChild(@SuppressWarnings("unused") Node child)
    {
        // Remove child
        if(this._textual_.remove(child))
        {
            return;
        }

        throw new RuntimeException("Not a child.");
    }

    @Override
    void replaceChild(@SuppressWarnings("unused") Node oldChild, @SuppressWarnings("unused") Node newChild)
    {
        // Replace child
        for(ListIterator<PTextual> i = this._textual_.listIterator(); i.hasNext();)
        {
            if(i.next() == oldChild)
            {
                if(newChild != null)
                {
                    i.set((PTextual) newChild);
                    newChild.parent(this);
                    oldChild.parent(null);
                    return;
                }

                i.remove();
                oldChild.parent(null);
                return;
            }
        }

        throw new RuntimeException("Not a child.");
    }
}
